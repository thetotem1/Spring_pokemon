spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pokemon_management
    username: postgres
    password: Test1234=
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 8080
